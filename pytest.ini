[pytest]
DJANGO_SETTINGS_MODULE = config.settings
testpaths = movies/tests
python_files = test_*.py *_tests.py
# pytest-django uses test_tutorial (see settings DATABASES TEST NAME).
# Seed migration runs there too; movies/tests/conftest.py clears tables per test.
filterwarnings =
    ignore::DeprecationWarning